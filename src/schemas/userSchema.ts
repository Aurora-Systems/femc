export interface User {
   user_id:string,
   first_name:string,
   last_name:string,
   organization:boolean,
   email:string,
   contact_number:string,
   organization_name:string|null,
   is_admin:boolean

}